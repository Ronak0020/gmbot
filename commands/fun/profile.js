const Discord = require("discord.js");
const fs = require('fs');

module.exports = {
    name: 'prefix',
    aliases: ["configprefix", "changeprefix"],
    category: "moderation",
    run: async (client, message, args) => {
        
    }
          }
    